# Candidate Background — Saiakhil Chilaka

Cornell CS sophomore (May 2028). Use this as context only — do not paste bullets into the email.

## What He's Built

**VHB:** PDF-to-form pipeline for crash reports using Claude API + RAG classification, cutting analyst time 66% across 10k+ annual cases. PyQt6 GUI with Playwright automation.

**PersonaAI:** 3-stage GPT-4 pipeline for synthetic research personas with real-time interview simulation. Replaced 2–3 week manual cycles with sub-5-minute runs. Serverless backend with 40% token reduction via dynamic context selection.

**Cassandra:** 7-stage autonomous prediction engine for Polymarket (live paper trading). Hybrid RAG over Neo4j + Qdrant with BM25/vector/graph fusion and BGE cross-encoder reranker. LLM judge pipeline scoring groundedness and bias detection.

**ShadowStrike:** Real-time multiplayer fighting game in Rust/WASM with rollback netcode, 304-byte game states, deterministic simulation, 50ms P95 latency on Cloudflare edge.

**Legion:** Distributed AI agent orchestration in Go — exactly-once execution via PostgreSQL advisory locks, embedded SFTP with ACL, WebSocket fan-out with sub-8µs propagation to 100 concurrent subscribers.

**FlavorNet:** GNN for ingredient compatibility using molecular data + InfoNCE loss. 3-layer heterogeneous GAT on 436k pairs with active learning loop.

## Stack
Python, TypeScript, Go, Rust · React, Next.js, FastAPI, PyTorch · PostgreSQL, Redis, Supabase, Docker · Claude API, WebSockets, WASM
